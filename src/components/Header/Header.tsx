import { AppBar, Box, Toolbar } from "@mui/material";

const Header = () => {
  return (
    <AppBar>
      <Toolbar>
        <Box>box</Box>
        <Box>box</Box>
      </Toolbar>
    </AppBar>
  );
};

export default Header;
